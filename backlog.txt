1. [x] If the client had not previously joined other servers running italy, it would download the map but some textures were replaced with placeholders.
   Fix: `dppb2_map_init` now syncs base textures (`pball`, `sfx`), fetches Italy-specific assets from `dplogin/files/textures/*`, and `./pball/textures` is mounted into the main server container.

2. [ ] Curate a default casual map rotation preset: small layouts suited to low player counts, no ground-spawned weapons, and no maps where CO2/oxygen capsules force an awkward reload cycle at round start.

3. [x] Split bot settings into a dedicated config (mounted or synced like other server configs) so every bot-related cvar/option exposed by the game can be tuned without hunting through `server.cfg` or image defaults.
   Fix: Superseded by unified `config.yaml` + `render_config.py`; bot cvars are under `gameplay.bots` in YAML.

4. [ ] Investigate whether annoying mechanics (e.g. oxygen/CO2 capsule pickups and related round-start friction) can be disabled or mitigated via server cvars, game code, or map design—without breaking balance for players who expect stock behavior.

5. [x] Add warnings to generated config files that they are auto-generated and should not be edited by hand—point people to `config.yaml` instead.